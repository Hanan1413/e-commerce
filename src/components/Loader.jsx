// Loader.js
import React from 'react';

const Loader = () => (
  <div className="loader">
    <p>Loading...</p>
    {/* Optionally, add a spinner icon here */}
  </div>
);

export default Loader;
